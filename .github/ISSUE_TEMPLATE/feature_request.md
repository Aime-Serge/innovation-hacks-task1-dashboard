---
name: Feature request
about: Propose a change or addition to DevDash
title: "feat: <short description>"
labels: enhancement
---

## Problem

What is missing or awkward today?

## Proposed solution

What should change? Note any UI states affected (loading, empty, error, both
themes, mobile drawer).

## Alternatives considered

## Requirement / test ID

If this maps to a tracked requirement, give its ID from
`docs/traceability.md` (e.g. `FR-##`, `NFR-##`). This repository's Engineering
Standards Pack file was not found under `docs/standards/` at the time this
template was added, so leave this blank if you don't have one.

## Tests

Which tests will need to be added or updated (`tests/unit`, `tests/contract`,
`tests/e2e`, `tests/a11y`)? Name the `TC-###` if one already exists.

## Screenshots / mockups

Attach if the change is visual.
